package interpreter;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class ParserTest {
    // idéalement il faudrait tester
    // - expression simples à une seule opération binaire
    // - expression simple à opération unaire
    // - expression avec successions d'opérations (sans problème de priorités)
    // - expression avec succession d'opérations (problème de priorité)
}
